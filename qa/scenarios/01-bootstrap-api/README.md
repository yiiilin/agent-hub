# Fresh Bootstrap API

Type: `api`

This scenario checks a newly created QA environment without starting a browser.
It verifies health, the seeded administrator, an online Runtime, the seeded
System Default Model Connection, migration version 1, and required singleton
control rows.
