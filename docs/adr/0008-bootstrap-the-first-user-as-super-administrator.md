# Bootstrap the first user as Super Administrator

An empty Agent Hub makes the first Hub User successfully created through any enabled registration or trusted Authentication Channel its initial Super Administrator, using an atomic first-user decision. Agent Hub deliberately requires no deployment token or separate setup ceremony, accepting first-arrival takeover risk in exchange for direct zero-configuration registration and login.
