# Bind each Session to one Agent

A Session is permanently assigned to the Agent selected when it is created. Agent Hub does not switch an existing Session to another Agent because its Session History, Workspace, instructions, Skills, MCP credentials, and permissions form one continuing trust boundary; using another Agent requires a new Session rather than mutating that boundary.
